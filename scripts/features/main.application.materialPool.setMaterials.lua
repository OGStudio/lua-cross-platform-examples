FEATURE main.lua/Impl
main.application.materialPool.setMaterials = function(self, definition)
    -- TODO Parse `definition` table into loadResource and other calls?
    print("TODO", "setMaterials()")
end
